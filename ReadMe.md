# Assessment 01 (Version B)

## Problem 01 (33%)

```
/**
 * Given an int n, return true if the absolute difference between n and 100 is less
 * than or equal to 10.
 * diff100(50) → false
 * diff100(90) → true
 * diff100(105) → true
 * diff100(89) -> false
 */
public Boolean diff100(int n) {
    return null;
}
```

## Problem 02 (33%)

```
/**
 * Return the string 'three' if the given non-negative number is a multiple of 3
 * or the string 'five' if the given non-negative number is a multiple of 5
 * or the string 'both' if the given non-negative number is a multiple of 3 and 5
 * or the string 'neither' if it's not a multiple of either
 * % "mod" operator.
 * threeOr5or3and5(3) → 'three'
 * threeOr5or3and5(10) → 'five'
 * threeOr5or3and5(8) → 'neither'
 * threeOr5or3and5(15) → 'both'
 */
public String threeOr5or3and5(Integer n) {
    return null;
}
```

## Problem 03 (33%)

```
/**
 * Given 2 ints, a and b, return true if one if them is 10 or 20 also if their sum is 10 or 20.
 * makes10or20(9, 10) → true
 * makes10or20(9, 9) → false
 * makes10or20(1, 9) → true
 * makes10or20(20,3) → true
 * makes10or20(18,2) → true
 */

public boolean makes10or20(int a, int b) {
    return false;
}
```

## Problem 04 (Bonus points)

```
/**
 * Bonus Question
 * Given 2 int values, return true if one is negative and one is positive.
 * Except if the parameter "negative" is true, then return true only if both are negative.
 * posNeg(1, -1, false) → true
 * posNeg(-1, 1, false) → true
 * posNeg(-4, -5, true) → true
 */

public boolean posNeg(int a, int b, boolean negative) {
    return false;
}
```